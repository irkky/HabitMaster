# Production environment variables for Railway deployment

# MongoDB Atlas connection string (replace with your actual Atlas connection string)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/habitmaster?retryWrites=true&w=majority

# JWT Secret (generate a strong secret for production)
JWT_SECRET=your-super-secure-jwt-secret-key-here

# JWT Configuration
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30

# Port (Railway automatically sets this)
PORT=8000

# Note: 
# 1. Replace MONGODB_URI with your actual MongoDB Atlas connection string
# 2. Generate a strong JWT_SECRET using: python -c "import secrets; print(secrets.token_urlsafe(32))"
# 3. These variables will be set in Railway dashboard, not in this file